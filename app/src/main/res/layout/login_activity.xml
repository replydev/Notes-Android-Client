<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".activities.L">

    <EditText
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:inputType="textPersonName"
            android:text="@string/Username"
            android:ems="10"
            android:id="@+id/usernameText"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginTop="155dp" app:layout_constraintStart_toStartOf="parent"
            android:layout_marginStart="80dp" android:autofillHints=""/>
    <EditText
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:inputType="textPassword"
            android:ems="10"
            android:id="@+id/passwordText"
            app:layout_constraintStart_toStartOf="parent" android:layout_marginStart="80dp"
            app:layout_constraintTop_toBottomOf="@+id/usernameText" android:layout_marginTop="28dp"
            android:autofillHints="" app:layout_constraintBottom_toTopOf="@+id/loginButton"/>
    <Button
            android:text="@string/login"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" android:id="@+id/loginButton"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginBottom="376dp" android:layout_marginEnd="180dp"
            app:layout_constraintEnd_toEndOf="parent" android:onClick="loginButton"/>
</androidx.constraintlayout.widget.ConstraintLayout>